{
  "id": "device-pair",
  "name": "Device Pairing",
  "description": "Share gateway connection info and approve device pairing requests.",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "publicUrl": {
        "type": "string"
      }
    }
  },
  "uiHints": {
    "publicUrl": {
      "label": "Gateway URL",
      "help": "Public WebSocket URL to share with /pair (ws/wss or http/https)."
    }
  }
}
